SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key

# Edge Function URL that proxies the AI Prayer Assistant to Claude. Never put
# an Anthropic API key directly in this app — it must live server-side as a
# Supabase Edge Function secret. See README.md for setup.
ANTHROPIC_ASSISTANT_PROXY_URL=
